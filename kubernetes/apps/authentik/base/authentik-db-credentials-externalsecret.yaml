apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: authentik-db-credentials # name of the ExternalSecret
  namespace: authentik
spec:
  refreshInterval: 8h
  secretStoreRef:
    kind: ClusterSecretStore
    name: azure-store

  target:
    name: authentik-db-credentials  # K8s Secret name to create
    template:
      type: kubernetes.io/basic-auth
    creationPolicy: Owner

  data:
  # name of the SECRET in the Azure KV (no prefix is by default a SECRET)
  - secretKey: username
    remoteRef:
      key: authentik-db-username
  - secretKey: password
    remoteRef:
      key: authentik-db-password

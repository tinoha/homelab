apiVersion: v1
kind: ConfigMap
metadata:
  name: kong-helm-values
  namespace: kong
data:
  values.yaml: |
    crds:
      enabled: true
    proxy:
      type: LoadBalancer
      annotations:
        metallb.universe.tf/address-pool: proxy-ip-pool  # Use the MetalLB address pool named "proxy-ip-pool"
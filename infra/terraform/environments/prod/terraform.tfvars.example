# Variables are used for naming and tagging the resources
# These can help to make namings globally unique where needed.

project     = "<your_project_name>"
environment = "prod"
name_suffix = "" # example 01,02...
load_secrets = false